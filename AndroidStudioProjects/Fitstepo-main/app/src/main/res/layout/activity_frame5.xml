<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF"
    android:padding="24dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Заголовок -->
        <TextView
            android:id="@+id/titleText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="What goal do you want to achieve?"
            android:textSize="18sp"
            android:textColor="#000000"
            android:fontFamily="sans-serif-medium"
            android:layout_marginBottom="8dp" />

        <!-- Підпис -->
        <TextView
            android:id="@+id/subtitleText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="You can choose more than one."
            android:textSize="14sp"
            android:textColor="#666666"
            android:layout_marginBottom="24dp"
            android:fontFamily="sans-serif" />

        <!-- Опції -->
        <include layout="@layout/item_goal_option" android:id="@+id/optionWeightLoss" />
        <include layout="@layout/item_goal_option" android:id="@+id/optionWeightGain" />
        <include layout="@layout/item_goal_option" android:id="@+id/optionMuscleBuilding" />
        <include layout="@layout/item_goal_option" android:id="@+id/optionStrengthTraining" />
        <include layout="@layout/item_goal_option" android:id="@+id/optionFlexibility" />
        <include layout="@layout/item_goal_option" android:id="@+id/optionEndurance" />
        <include layout="@layout/item_goal_option" android:id="@+id/optionBalance" />
        <include layout="@layout/item_goal_option" android:id="@+id/optionPress" />

        <!-- Кнопки -->
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="horizontal"
            android:layout_marginTop="24dp"
            android:gravity="center">

            <Button
                android:id="@+id/btnBack"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Back" />

            <Space
                android:layout_width="16dp"
                android:layout_height="wrap_content" />

            <Button
                android:id="@+id/btnNext"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:text="Next" />
        </LinearLayout>

    </LinearLayout>
</ScrollView>
